# Aptos Build (geomi.dev) API key, Devnet network. Free, no waitlist.
# Without this, scripts/deploy.ts and scripts/demo.ts hit devnet's public
# per-IP rate limit (40000 compute units / 300s) fast. See docs/DECISIONS.md.
NODE_API_KEY=
